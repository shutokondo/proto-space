$('#comment_area').val('');
$('#comments').html(
  '<%= j(render partial: 'prototypes/comment', collection: @prototype.comments) %>'
);
$('#comment-sum').html(
  '<%= j("#{@prototype.comments.count}") %>');
